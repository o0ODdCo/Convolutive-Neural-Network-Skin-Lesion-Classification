# Raw experiment logs

Optional folder for detailed raw experiment logs.

The supplied `batch_metrics_model1.csv` contains batch-level metrics and is not included here by default.

If you decide to version large experiment artifacts, consider an external experiment tracker, release asset, cloud storage, or Git LFS rather than ordinary Git commits.
