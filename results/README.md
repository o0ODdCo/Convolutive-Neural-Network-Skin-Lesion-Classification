# Results

Included:
- `epoch_metrics_model1.csv`: one summary row per training epoch.
- `figures/loss_curve.png`
- `figures/macro_f1_curve.png`

Still recommended:
- `figures/confusion_matrix.png`
- a short table comparing multiple model configurations if you train several models.

The large batch-by-batch CSV is intentionally not included in the upload-ready package because it is much heavier and less useful for a quick GitHub review. You may archive it separately if needed.
